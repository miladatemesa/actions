name: sample-workflow
on: ['push']
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: first-step
        uses: actions/hello-world-javascript-action
        with:
          who-to-greet: Milad